Security currently applied to the contact form:
- hidden honeypot field
- strict field lengths and validation
- request body size limit
- Cloudflare Turnstile challenge
- mandatory server-side Siteverify validation
- Turnstile action check (contact)
- Turnstile hostname check against the live request hostname
- single-use, short-lived Turnstile tokens
- Resend API key remains server-side only
- Turnstile secret remains server-side only
- CSP allows only the required Cloudflare Turnstile origins
- public inbox address is not exposed in HTML/JavaScript; CONTACT_TO_EMAIL stays server-side

Production hostname:
- v3breaks.com

Keep account 2FA enabled for Cloudflare, GitHub and Resend.
